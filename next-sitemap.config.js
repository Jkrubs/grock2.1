/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://greenrock.co.ke',
  generateRobotsTxt: true, // Automatically generates a robots.txt file as well
  // Optional: exclude specific paths you don't want indexed
  // exclude: ['/admin/*', '/protected-page'], 
};